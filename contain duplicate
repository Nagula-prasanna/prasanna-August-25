class Solution(object):
    def containsDuplicate(self, nums):
        ans = set()
        for num in nums:
            if num in ans:
                return True
            ans.add(num)
        return False

