class Solution(object):
    def addDigits(self, num):
        while num >= 10:
            sum_digits = 0
            temp = num
            while temp > 0:
                sum_digits += temp % 10
                temp //= 10
            num = sum_digits
        return num
